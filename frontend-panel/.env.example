# Usa una ruta relativa para pasar por el proxy de Vite en desarrollo y evitar CORS.
# En produccion, /api apunta al gateway publicado en el mismo dominio.
VITE_API_URL=/api
# true permite revisar las pantallas con datos locales sin reemplazar los POST reales.
VITE_USE_DEMO_DATA=false
